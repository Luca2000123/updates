name: build-site

on:
  push:
    branches: [main]
    paths:
      - apps/**
      - releases/**
      - schema/**
      - static/**
      - tools/**
      - hub.toml
      - .github/workflows/build-site.yml
  pull_request:
    paths:
      - apps/**
      - releases/**
      - schema/**
      - tools/**
      - hub.toml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - run: pip install --disable-pip-version-check jsonschema

      - name: Test del generatore
        run: python tools/test_hub.py

      - name: Genera il sito
        run: python tools/build_manifests.py --out site --strict

      - name: Valida i manifest contro lo schema
        run: python tools/validate_site.py --site site

      # Solo push/workflow_dispatch pubblicano: una PR deve solo validare.
      - name: Prepara l'artifact per Pages
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Pubblica su GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
